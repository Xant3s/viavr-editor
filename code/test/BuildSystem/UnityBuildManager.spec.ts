// import UnityPackageManager from '../../src/App/BuildSystem/UnityPackageManager'
import {describe, it} from 'mocha'
import {expect} from 'chai'

describe('UnityBuildManager', () => {
    describe('Test addScopedRegistryToManifest', () => {
        it('should create a array of scoped registries if there is none', () => {
            // const UnityBuildManager = new UnityBuildManager(null)
            expect(1).to.equal(1)
        })
    })
})
